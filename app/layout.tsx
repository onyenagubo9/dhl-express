import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DHL Express",
  description: "DHL-Express Delivery App is a fast, secure, and reliable logistics platform designed to simplify global and local shipping. It enables users to send, track, and manage deliveries in real-time, ensuring packages arrive safely and on time. With seamless tracking, instant notifications, and efficient courier services, we bring speed and convenience to your doorstep.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
